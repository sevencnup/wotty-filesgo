{
  "name": "cloud-system-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:go\"",
    "dev:frontend": "cd frontend && npm install && npm run dev",
    "dev:go": "cd server-go && go run .",
    "dev:rust": "echo Rust worker skipped (optional)",
    "build": "npm run build:frontend && npm run build:prep && npm run build:go",
    "build:frontend": "cd frontend && npm run build",
    "build:prep": "powershell -Command \"Remove-Item -Recurse -Force server-go/dist -ErrorAction SilentlyContinue; Copy-Item -Recurse frontend/dist server-go/dist\"",
    "build:go": "cd server-go && go build -o cloud-server.exe"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
