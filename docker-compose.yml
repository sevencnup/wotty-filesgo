version: "3.9"
services:
  filesgo:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./data/uploads:/app/uploads
      - ./data/filesgo.db:/app/filesgo.db
    environment:
      - GIN_MODE=release
