# FilesGO 主题改造开发文档（2026-05-20）

## 需求目标
将 `frontend` 页面主题改为参考图标风格：浅雾灰背景、蓝青渐变主视觉、橙黄点缀、柔和立体阴影。

## 评估结论
本次属于跨页面视觉体系调整（首页 + 设备页 + 全局主题），影响范围较大，
需要先形成开发文档并按步骤实施。

## 执行步骤
1. 梳理现有主题入口（`layout.tsx`、`globals.css`、`page.tsx`、`device/page.tsx`）。
2. 定义全局设计变量（颜色、渐变、阴影、圆角、输入框与按钮统一风格）。
3. 落地首页主题（玻璃卡片、上传区、按钮、输入框、取件码展示）。
4. 落地设备页主题（侧栏、聊天气泡、弹窗、操作按钮与输入框）。
5. 运行构建与校验命令，记录风险项与结果。

## 实施结果
- 已完成步骤 1-5。
- 构建通过：`npm run build` 成功。
- Lint 存在历史问题（any、未使用变量、hooks 依赖提示），本次未扩展修复逻辑。

## 影响文件
- `frontend/src/app/layout.tsx`
- `frontend/src/app/globals.css`
- `frontend/src/app/page.tsx`
- `frontend/src/app/device/page.tsx`

## 回滚方案
若需回滚，仅恢复上述文件到上一个提交版本即可。
