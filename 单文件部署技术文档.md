# Go + Vue 单文件部署技术方案文档

本方案通过 Go 语言的 `embed` 特性，将前端静态资源（HTML/JS/CSS/图片）直接打包进后端二进制文件中，实现“一个文件即可部署”的高效方案。

---

## 1. 技术栈
*   **前端**：Vue 3 + Vite + Tailwind CSS
*   **后端**：Go (Golang) + Gin 框架
*   **核心指令**：`//go:embed`

---

## 2. 核心原理
1.  **前端构建**：通过 Vite 将 Vue 项目编译为纯静态的 `dist` 文件夹。
2.  **静态嵌入**：Go 编译器在编译时扫描 `dist` 目录，将其二进制数据读入内存。
3.  **文件系统映射**：使用 `http.FS` 将内存中的数据映射为 Web 服务器的静态路由。

---

## 3. 实现步骤

### 第一步：Go 代码配置 (main.go)
在后端主文件中引入 `embed` 包，并声明嵌入路径。

```go
package main

import (
    "embed"
    "io/fs"
    "net/http"
    "github.com/gin-gonic/gin"
)

// 1. 声明嵌入 dist 文件夹下的所有内容
//go:embed dist/*
var frontendAssets embed.FS

func main() {
    r := gin.Default()

    // 2. 获取子目录 fs.FS 对象（去掉 dist 前缀）
    distFS, _ := fs.Sub(frontendAssets, "dist")

    // 3. 配置静态资源路由
    // 访问 /assets/xxx 会直接从内存中读取
    r.StaticFS("/assets", http.FS(distFS))

    // 4. 处理单页应用 (SPA) 路由
    r.NoRoute(func(c *gin.Context) {
        // 所有未匹配的路由都返回 index.html
        data, _ := distFS.ReadFile("index.html")
        c.Data(http.StatusOK, "text/html; charset=utf-8", data)
    })

    r.Run(":8080")
}
```

### 第二步：自动化构建脚本
为了方便，通常使用一个脚本（如 `build.sh` 或 PowerShell）完成全套流程。

#### Windows (PowerShell):
```powershell
# 1. 构建前端
cd ../frontend
npm run build

# 2. 拷贝资源到后端目录
cd ../server-go
Remove-Item -Recurse -Force dist
Copy-Item -Recurse ../frontend/dist .

# 3. 交叉编译为 Linux 可执行文件
$env:GOOS='linux'
$env:GOARCH='amd64'
go build -o my-app-linux main.go
```

---


## 4. 方案优势
*   **原子化发布**：前端和后端逻辑同步更新，不会出现版本不匹配的情况。
*   **零环境依赖**：服务器不需要安装 Node.js、Nginx 或任何运行库。
*   **部署极简**：只需 `scp` 上传一个文件，配合 `systemd` 或 `supervisor` 即可完成部署。
*   **性能优异**：资源直接从内存读取（由 Go 内部维护缓存），响应速度极快。

---

## 5. 注意事项
1.  **文件大小**：如果前端图片或视频非常多（超过 100MB），会导致生成的二进制文件过大，启动变慢。建议大资源文件使用 CDN。
2.  **路径一致性**：`//go:embed` 的路径是相对于当前 `.go` 文件的相对路径。
3.  **开发模式**：在开发环境下，建议使用 Gin 的 `r.Static()` 映射本地文件夹，以便实现前端热更新；生产环境才使用 `embed`。
